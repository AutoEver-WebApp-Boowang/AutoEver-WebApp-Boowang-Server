spring:
  application:
    name: boowang
  datasource:
    url: jdbc:mysql://localhost:3306/boowang?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: 7070
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true